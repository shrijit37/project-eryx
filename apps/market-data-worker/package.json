{
    "name": "@project-eryx/market-data-worker",
    "private": true,
    "scripts": {
        "dev": "uv run python src/main.py",
        "build": "echo 'No build required'",
        "format": "ruff format .",
        "test": "python3 -m compileall -q src"
    }
}