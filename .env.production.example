# Production environment template (server-side only — never commit real values).
# These values are set in Dokploy's compose environment (project-eryx/dc).
# DATABASE_URL uses the compose service name `db`, NOT localhost (Docker networking).

DATABASE_URL=postgresql://postgres:postgres@db:5432/project_eryx?schema=public
REDIS_HOST=redis
JWT_SECRET=change-me
PORT=8080
WS_PORT=4040
FINNHUB_API=
TRIGGER_MS=3000
RECONCILE_MS=900000